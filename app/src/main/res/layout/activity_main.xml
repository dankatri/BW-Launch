<?xml version="1.0" encoding="utf-8"?>
<!--
    E-INK OPTIMIZED LAYOUT
    
    - No transparency or gradients
    - Pure black/white colors
    - Large tap targets (56-64dp minimum)
    - Solid color backgrounds
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/rootLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?android:attr/colorBackground"
    android:layerType="software"
    tools:context=".MainActivity">

    <!-- Main home screen content -->
    <LinearLayout
        android:id="@+id/homeContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:clickable="true"
        android:focusable="true">

        <!-- Favorite apps list - uses custom EInkRecyclerView -->
        <com.bwlaunch.launcher.util.EInkRecyclerView
            android:id="@+id/favoritesRecyclerView"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:clipToPadding="false"
            android:scrollbars="none"
            android:overScrollMode="never"
            android:layerType="software" />

        <!-- Empty state message -->
        <com.bwlaunch.launcher.util.EInkTextView
            android:id="@+id/emptyStateText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/no_apps_configured"
            android:textSize="16sp"
            android:textColor="@color/eink_gray"
            android:visibility="gone"
            android:layout_gravity="center" />

    </LinearLayout>

    <!-- Weather widget - top right corner -->
    <com.bwlaunch.launcher.util.EInkTextView
        android:id="@+id/weatherText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp"
        android:padding="8dp"
        android:textSize="14sp"
        android:textColor="?android:attr/textColorPrimary"
        android:visibility="gone"
        android:contentDescription="@string/cd_weather_widget"
        tools:text="72\u00B0 Clear"
        tools:visibility="visible" />

    <!-- All Apps button - subtle icon in bottom right corner -->
    <ImageButton
        android:id="@+id/allAppsButton"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="16dp"
        android:src="@drawable/ic_grid"
        android:background="?android:attr/selectableItemBackgroundBorderless"
        android:padding="12dp"
        android:scaleType="fitCenter"
        android:contentDescription="@string/cd_open_all_apps" />

    <!-- All Apps overlay (initially hidden) -->
    <FrameLayout
        android:id="@+id/allAppsOverlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?android:attr/colorBackground"
        android:visibility="gone">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical">

            <!-- Header with close button - large tap target -->
            <FrameLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:minHeight="64dp"
                android:padding="16dp">

                <com.bwlaunch.launcher.util.EInkTextView
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:text="@string/all_apps"
                    android:textSize="20sp"
                    android:textStyle="bold"
                    android:textColor="?android:attr/textColorPrimary" />

                <ImageButton
                    android:id="@+id/closeAllAppsButton"
                    android:layout_width="56dp"
                    android:layout_height="56dp"
                    android:layout_gravity="end|center_vertical"
                    android:background="@drawable/eink_selectable_item"
                    android:contentDescription="@string/cd_close_drawer"
                    android:src="@drawable/ic_close"
                    android:padding="14dp"
                    android:importantForAccessibility="yes" />

            </FrameLayout>

            <!-- Divider - solid black line -->
            <View
                android:layout_width="match_parent"
                android:layout_height="2dp"
                android:background="?android:attr/textColorPrimary" />

            <!-- All apps grid - uses custom EInkRecyclerView -->
            <com.bwlaunch.launcher.util.EInkRecyclerView
                android:id="@+id/allAppsRecyclerView"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:padding="8dp"
                android:clipToPadding="false"
                android:scrollbars="none"
                android:overScrollMode="never"
                android:layerType="software" />

        </LinearLayout>

    </FrameLayout>

</FrameLayout>
